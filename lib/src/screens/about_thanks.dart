import 'package:flutter/material.dart';

class AboutThanks extends StatelessWidget {

    Widget build(BuildContext context) {
        return Scaffold(
            body: Center(
                child: Text('About Thanks'),
            ),
        );
    }

}

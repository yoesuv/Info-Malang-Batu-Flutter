import 'package:flutter/material.dart';

class AboutLibraries extends StatelessWidget {

    Widget build(BuildContext context) {
        return Scaffold(
            body: Center(
                child: Text('About Libraries'),
            ),
        );
    }

}

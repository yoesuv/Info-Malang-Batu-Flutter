import 'package:flutter/material.dart';

class AboutChangelog extends StatelessWidget {

    Widget build(BuildContext context) {
        return Scaffold(
            body: Center(
                child: Text('About Changelog'),
            ),
        );
    }

}
